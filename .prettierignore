# Generated — never hand-edit; regenerated by `npm run api` / sqlc / openapi-typescript
frontend/src/api/schema.ts
backend/internal/httpd/apispec/openapi.yaml

# Build outputs
frontend/dist
frontend/dist-electron
frontend/release
frontend/test-results
frontend/playwright-report

# Lockfiles — generated, and the format job commits its output back, so
# leaving pnpm-lock.yaml in scope means every push rewrites ~2900 lines of a
# file pnpm owns.
package-lock.json
frontend/package-lock.json
pnpm-lock.yaml

# Go uses gofmt, not Prettier
backend/

# GitHub refuses a GITHUB_TOKEN-authenticated push that touches workflow files,
# so the format job's auto-commit is rejected outright the moment Prettier
# reformats one of these. Formatting them is not worth losing the whole job.
.github/workflows/
